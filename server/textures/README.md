# FutureCast hero textures (optional)

Place these PNG files here for hero overlays on FutureCast elite pages:

- `stadium-lights.png` — top stadium light bloom
- `swamp-mist.png` — bottom mist layer

CSS in `client/lib/futurecast-elite.css` uses these paths with gradient fallbacks when files are missing.
