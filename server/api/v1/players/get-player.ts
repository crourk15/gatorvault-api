/**
 * GET /api/players/:id — Player Profiles 2.0
 * @deprecated Use server/api/players/[id].ts + profiles + signals (Phase 3 mount)
 */
export { handleGetPlayerById as handleGetPlayer } from '../../players/[id]';
