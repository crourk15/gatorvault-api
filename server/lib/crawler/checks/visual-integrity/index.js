/**
 * Visual integrity — React vault theme/layout (delegates to visual-integrity module).
 */
const { runVisualIntegrityChecks } = require('../../../visual-integrity/visual-integrity-checks');

module.exports = { runVisualIntegrityChecks };
