/** GatorVault Detectives */
const { SITE_URL } = require('./discovery-core');
const { intelFingerprint } = require('../commit-fingerprint');
const store = require('./detectives-store');
const platform = require('./detectives-platform');
const handoff = require('./detectives-handoff');
const ANGLES = ['visit_intel','rpm_board','scouting_read','competition','momentum','program_signal'];
function detectivesEnabled(){return process.env.X_AUTOPOST_DETECTIVES_ENABLED!=='false';}
function logDetectives(d={}){try{require('../ops-monitor').logEvent({subsystem:'autoposter:detectives',status:d.ok?'success':d.status||'skipped',message:d.ok?'detectives:resolved':'detectives:'+(d.phase||'event'),details:d});}catch{}}
function beatTextFromCase(c){const beat=c?.beatPost||{};const cand=c?.candidate||{};return String(beat.text||beat.summary||cand.beatText||cand.text||cand.triggerPhrase||'').trim();}
function extractHints(caseItem){const beat=caseItem?.beatPost||{};const cand=caseItem?.candidate||{};const text=beatTextFromCase(caseItem);let playerName=cand.playerName||caseItem?.hints?.playerName||null;let playerSlug=cand.playerSlug||caseItem?.hints?.playerSlug||null;let classYear=cand.classYear||caseItem?.hints?.classYear||null;try{const copy=require('../x-autoposter-copy');if(!playerName&&text)playerName=copy.extractPlayerFromText(text);if(playerName&&!playerSlug)playerSlug=require('../slug').slugify(playerName);}catch{}const m=text.match(/\b(20(?:2[6-9]|3[0-2]))\b/);if(!classYear&&m)classYear=parseInt(m[1],10);return{beatText:text,playerName,playerSlug,classYear,handle:beat.handle||caseItem?.hints?.handle||null,writerName:beat.writerName||beat.outlet||caseItem?.hints?.writerName||null,url:beat.url||cand.sources?.[0]?.url||null,publishedAt:beat.publishedAt||cand.sourcePublishedAt||null,skipReason:caseItem?.skipReason||null,triggerPhrase:cand.triggerPhrase||text.slice(0,160),pos:cand.pos||caseItem?.hints?.pos||null};}
async function resolveIdentity(hints){const out={playerName:hints.playerName,playerSlug:hints.playerSlug,classYear:hints.classYear,sources:[]};try{const lookup=require('../player-identity-lookup');const resolved=await lookup.enrichAndConfirmIntelIdentity({playerName:hints.playerName,beatText:hints.beatText,classYear:hints.classYear,intel:{detail:hints.beatText,playerName:hints.playerName}});if(resolved?.confirmed&&resolved.mergedSnapshot){out.playerName=resolved.mergedSnapshot.playerName||out.playerName;out.playerSlug=resolved.mergedSnapshot.playerSlug||out.playerSlug;out.classYear=resolved.mergedSnapshot.classYear||out.classYear;out.pos=resolved.mergedSnapshot.pos||hints.pos;out.on3Id=resolved.mergedSnapshot.on3Id;out.stars=resolved.mergedSnapshot.stars;out.natlRank=resolved.mergedSnapshot.natlRank;out.ufRpmPct=resolved.mergedSnapshot.ufRpmPct;out.sources.push('intel-identity-lookup');}else if(resolved?.mergedSnapshot?.playerName){out.playerName=resolved.mergedSnapshot.playerName;out.playerSlug=resolved.mergedSnapshot.playerSlug||out.playerSlug;}}catch{}if(!out.playerSlug&&out.playerName){try{const auto=require('../recruiting-auto-resolution');const pack=await auto.autoResolveRecruitingIntel({playerName:out.playerName,beatText:hints.beatText,classYear:hints.classYear});if(pack?.resolved&&pack.mergedSnapshot){out.playerSlug=pack.mergedSnapshot.playerSlug||pack.playerSlug;out.playerName=pack.mergedSnapshot.playerName||out.playerName;out.sources.push('auto-resolution');}}catch{}}return out;}
function finalizeDetectivesText(text){try{const template=require('../x-autoposter-template');return template.enforceTweetLimit(String(text||''),280,{eliteMode:true,postKind:'detectives'})||String(text||'').slice(0,280);}catch{return String(text||'').slice(0,280);}}
function formatResearchContextLine(ufPosition,beatText,hints,identity={}){const elite=require('../x-autoposter-elite-caption');const school=elite.extractSchoolFromBeat(beatText);return elite.buildBeatFallbackBlocks({playerName:identity.playerName||hints?.playerName,beatText,classYear:identity.classYear||hints?.classYear,pos:identity.pos||hints?.pos,school}).context;}
function formatBeatDrivenInsiderLine(beatText,hints={},identity={}){const bt=String(beatText||'').trim();if(bt.length<24)return null;const elite=require('../x-autoposter-elite-caption');const school=elite.extractSchoolFromBeat(beatText);return elite.buildBeatFallbackBlocks({playerName:identity.playerName||hints?.playerName,beatText,classYear:identity.classYear||hints?.classYear,pos:identity.pos||hints?.pos,school}).insider;}
function formatResearchInsiderLine(breakdownStory,beatText,hints={},identity={}){if(breakdownStory){const story=String(breakdownStory).trim();if(story.length>=40)return story.slice(0,140);}return formatBeatDrivenInsiderLine(beatText,hints,identity);}
function buildBeatDrivenCandidate(caseItem,hints,identity,platformContext){const name=identity.playerName;const slug=identity.playerSlug;const elite=require('../x-autoposter-elite-caption');const template=require('../x-autoposter-template');const school=elite.extractSchoolFromBeat(hints.beatText);const idLine=template.buildCompactRecruitingIdentity({classYear:identity.classYear,pos:identity.pos,name,school})||((identity.classYear?identity.classYear+' ':'')+name+(identity.pos?' '+identity.pos:'')).trim()||'Florida recruiting intel';const blocks=elite.buildBeatFallbackBlocks({playerName:name,beatText:hints.beatText,classYear:identity.classYear,pos:identity.pos,school});const url=platformContext?.url||platform.resolvePlayerPlatformUrl(slug,false);return markDetectivesCandidate({text:[idLine,blocks.context,blocks.insider,url].join('\n'),category:'news',topic:'recruiting',urgencyLabel:'major_beat',sourceEventType:'detectives_beat_driven',sources:[{label:'On3',url:SITE_URL},{label:hints.writerName||'Beat',url:hints.url||SITE_URL}],templateBlocks:{identity:idLine,context:blocks.context,insider:blocks.insider}},caseItem,'beat_driven',hints,platformContext);}
function markDetectivesCandidate(raw,caseItem,pathTag,hints,platformContext){const ts=new Date().toISOString();const eventTs=hints?.publishedAt||raw.sourceEventCreatedAt||raw.sourcePublishedAt||ts;const slug=raw.playerSlug||hints.playerSlug||null;const name=raw.playerName||hints.playerName||null;const fp=intelFingerprint(caseItem.id,'detectives_'+(pathTag||'resolved'),ts.slice(0,10));const text=finalizeDetectivesText(raw.text);const beatDrivenOnly=!!(platformContext&&!platformContext.hasFutureCastContext);return Object.assign({},raw,{text,playerName:name,playerSlug:slug,intelFingerprint:raw.intelFingerprint||fp,identityConfirmed:raw.identityConfirmed!==false,sourceEventCreatedAt:eventTs,sourcePublishedAt:eventTs,qualityScore:Math.max(raw.qualityScore??0,88),sourceConfidence:Math.max(raw.sourceConfidence??0,85),source:raw.source||'auto:detectives',validationMeta:Object.assign({},raw.validationMeta||{},{detectivesResolved:true,detectivesCaseId:caseItem.id,detectivesPath:pathTag,eliteCompose:true,beatText:hints.beatText,beatSkipReason:caseItem.skipReason,platformPlayerReady:!!platformContext?.hasFutureCastContext,beatDrivenOnly,platformUrl:platformContext?.url||null,platformProvisioned:!!(platformContext?.provisioned||platformContext?.intelCreated),beatWriterTracked:true})});}
function buildGuaranteeCandidate(caseItem,hints,identity,angleIndex=0,platformContext){const angle=ANGLES[angleIndex%ANGLES.length];const name=identity.playerName;const slug=identity.playerSlug;const cy=identity.classYear?identity.classYear+' ':'';const pos=identity.pos?' '+identity.pos:'';const beatDrivenOnly=!!(platformContext&&!platformContext.hasFutureCastContext);if(name&&slug){const idLine=(cy+name+pos).trim();let contextLine,insiderLine,url;if(beatDrivenOnly){const copy=platform.beatOnlyCopyForAngle(angle);contextLine=copy.context;insiderLine=copy.insider;url=platformContext.url||platform.resolvePlayerPlatformUrl(slug,false);}else{const ctx={visit_intel:'Campus visit window confirmed — Florida had real face time with this target in Gainesville.',rpm_board:'RPM board read shows Florida in the mix on this name.',scouting_read:'Scout cross-check aligns with the beat momentum on FutureCast.',competition:'Competing schools tracked — Florida remains active in this race.',momentum:'Quiet momentum building on the Florida board.',program_signal:'Beat signal puts UF in play on a name staff is tracking.'};const ins={visit_intel:'OV timing and prediction shift live on FutureCast.',rpm_board:'Full RPM percentages updated on FutureCast.',scouting_read:'GV scout evaluation posted on the board.',competition:'Leaderboard and flip risk tracked on FutureCast.',momentum:'Heat check and staff priority read on FutureCast.',program_signal:'Player profile and visit log on FutureCast.'};contextLine=ctx[angle]||ctx.program_signal;insiderLine=ins[angle]||ins.program_signal;url=SITE_URL+'/vault/futurecast/player/'+slug;}return markDetectivesCandidate({text:[idLine,contextLine,insiderLine,url].join('\n'),category:'news',topic:'recruiting',urgencyLabel:'major_beat',sourceEventType:'detectives_intel',sources:[{label:'On3',url:SITE_URL},{label:hints.writerName||'Beat',url:hints.url||SITE_URL}],templateBlocks:{identity:idLine,context:contextLine,insider:insiderLine}},caseItem,'guarantee_player_'+angle,hints,platformContext);}const idLine='Florida recruiting intel';const contextLine='Beat intel confirmed UF football context on a prospect that failed first-pass filters.';const insiderLine=beatDrivenOnly?'Beat trail and player profile on Recruiting Hub.':'Board analysis and FutureCast breakdown rebuilt from beat signal.';const url=beatDrivenOnly?(platformContext?.url||SITE_URL+'/vault/recruiting'):SITE_URL+'/vault/futurecast';return markDetectivesCandidate({text:[idLine,contextLine,insiderLine,url].join('\n'),category:'news',topic:'program',urgencyLabel:'breaking',postUrgency:'breaking',triggerType:'program_news',programNewsType:'program_update',sourceEventType:'program_news',sources:[{label:'On3',url:SITE_URL},{label:hints.writerName||'Beat intel',url:hints.url||SITE_URL}],templateBlocks:{identity:idLine,context:contextLine,insider:insiderLine}},caseItem,'guarantee_program',hints,platformContext);}
async function buildStrategyCandidates(caseItem,hints,identity,platformContext){const strategies=[];const beatText=hints.beatText;const slug=identity.playerSlug;const name=identity.playerName;const beatDrivenOnly=!!(platformContext&&!platformContext.hasFutureCastContext);if(beatText&&(slug||name)){try{const eliteCaption=require('../x-autoposter-elite-caption');const built=await eliteCaption.buildElitePlayerPost({playerName:name,playerSlug:slug,beatText,publishedAt:hints.publishedAt,intel:{playerName:name,playerSlug:slug,eventType:'recruiting',detail:beatText,classYear:identity.classYear,sourceEventCreatedAt:hints.publishedAt,sourcePublishedAt:hints.publishedAt,publishedAt:hints.publishedAt},sourceLabel:hints.writerName||'Beat intel'});if(built?.ok&&built.text){strategies.push(markDetectivesCandidate({text:built.text,category:'news',topic:'recruiting',urgencyLabel:'major_beat',sourceEventType:'detectives_elite_caption',sources:[{label:'On3',url:SITE_URL},{label:hints.writerName||'Beat',url:hints.url||SITE_URL}],playerName:built.playerName||name,playerSlug:built.playerSlug||slug,templateBlocks:built.templateBlocks},caseItem,'elite_caption',hints,platformContext));}}catch{}}if(beatDrivenOnly&&beatText)strategies.push(buildBeatDrivenCandidate(caseItem,hints,identity,platformContext));if(!beatDrivenOnly){try{const research=require('../x-autoposter-elite-research');const pack=await research.researchUpdate({playerSlug:slug,playerName:name,beatText,sourceLabel:hints.writerName||'Beat',eventType:'recruiting'});if(pack?.hasUsableSignal&&platformContext?.hasFutureCastContext&&slug){const insiderLine=formatResearchInsiderLine(pack.breakdown?.recruitingStory,beatText,hints,identity);if(insiderLine){const contextLine=formatResearchContextLine(pack.ufPosition,beatText,hints,identity);const idLine=name?((identity.classYear?identity.classYear+' ':'')+name+(identity.pos?' '+identity.pos:'')).trim():'Florida recruiting target';const url=SITE_URL+'/vault/futurecast/player/'+slug;strategies.push(markDetectivesCandidate({text:[idLine,contextLine,insiderLine,url].join('\n'),category:'news',topic:'recruiting',urgencyLabel:'major_beat',sourceEventType:'detectives_elite_research',sources:[{label:'On3',url:SITE_URL}],playerName:pack.playerName||name,playerSlug:pack.playerSlug||slug,templateBlocks:{identity:idLine,context:contextLine,insider:insiderLine}},caseItem,'elite_research',hints,platformContext));}}}catch{}try{const ladder=require('./research-ladder');const raw={text:beatText,beatText,playerName:name,playerSlug:slug,source:'auto:beat-intel',sourceEventType:'beat_intel',sourceLabel:hints.writerName||'Beat',sources:[{label:'On3',url:SITE_URL}],sourceEventCreatedAt:hints.publishedAt||new Date().toISOString()};const ladderCand=await ladder.buildResearchLadderCandidate(raw,caseItem.skipReason||'beat_skip');if(ladderCand?.text)strategies.push(markDetectivesCandidate(ladderCand,caseItem,'research_ladder',hints,platformContext));}catch{}}if(slug){try{const entry=require('../scouting-database').getEntryBySlug(slug);if(entry?.scoutingSummary){const idLine=((identity.classYear?identity.classYear+' ':'')+(entry.playerName||name)+(entry.pos?' '+entry.pos:'')).trim();const contextLine='Florida scout file matches the beat momentum on this board name.';const insiderLine=String(entry.scoutingSummary).trim().slice(0,140);const url=beatDrivenOnly?(platformContext?.url||platform.resolvePlayerPlatformUrl(slug,false)):SITE_URL+'/vault/futurecast/player/'+slug;strategies.push(markDetectivesCandidate({text:[idLine,contextLine,insiderLine,url].join('\n'),category:'news',topic:'recruiting',urgencyLabel:'analysis',sourceEventType:'detectives_scouting',sources:[{label:'GatorVault Scout',url:SITE_URL}],playerName:entry.playerName||name,playerSlug:slug,templateBlocks:{identity:idLine,context:contextLine,insider:insiderLine}},caseItem,'scouting_db',hints,platformContext));}}catch{}}for(let i=0;i<ANGLES.length;i+=1)strategies.push(buildGuaranteeCandidate(caseItem,hints,identity,i,platformContext));const copy=require('../x-autoposter-copy');return strategies.filter((s)=>s?.text&&!copy.isBrokenCopy(s.text,s));}
async function tryEnqueueDetectivesCandidate(candidate,doc){const fill=require('../x-autoposter-fill');return fill.attemptEnqueueCandidate(candidate,doc,{skipDetectives:true,ladderDepth:99});}
async function investigateCase(caseItem,doc){if(!caseItem||caseItem.status==='resolved')return{ok:false,reason:'already_resolved'};const attempts=(caseItem.attempts||0)+1;store.updateCase(caseItem.id,{status:'investigating',attempts});store.appendLog(caseItem.id,{phase:'start',attempt:attempts,skipReason:caseItem.skipReason});const hints=extractHints(caseItem);const identity=await resolveIdentity(hints);store.appendLog(caseItem.id,{phase:'identity',playerName:identity.playerName,playerSlug:identity.playerSlug,sources:identity.sources});const platformContext=await platform.ensureBeatProspectOnPlatform({identity,hints,caseItem});if(platformContext.slug)identity.playerSlug=platformContext.slug;store.appendLog(caseItem.id,{phase:'platform',ok:platformContext.ok,slug:platformContext.slug,hasFutureCastContext:platformContext.hasFutureCastContext,intelCreated:platformContext.intelCreated,provisioned:platformContext.provisioned,url:platformContext.url});const strategies=await buildStrategyCandidates(caseItem,hints,identity,platformContext);store.appendLog(caseItem.id,{phase:'strategies',count:strategies.length,beatDrivenOnly:!platformContext.hasFutureCastContext});for(let i=0;i<strategies.length;i+=1){const candidate=strategies[i];const pathTag=candidate.validationMeta?.detectivesPath||('strategy_'+i);const result=await tryEnqueueDetectivesCandidate(candidate,doc);if(result?.queued){store.updateCase(caseItem.id,{status:'resolved',resolvedAt:new Date().toISOString(),resolvedPath:pathTag,resolvedCandidate:{text:candidate.text?.slice(0,280),playerSlug:candidate.playerSlug},queueItemId:result.item?.id||null});store.appendLog(caseItem.id,{phase:'resolved',path:pathTag,queueItemId:result.item?.id});logDetectives({ok:true,caseId:caseItem.id,path:pathTag,playerSlug:candidate.playerSlug,skipReason:caseItem.skipReason,beatDrivenOnly:!platformContext.hasFutureCastContext});return{ok:true,queued:true,path:pathTag,item:result.item};}store.appendLog(caseItem.id,{phase:'reject',path:pathTag,reason:result?.reason});}if(attempts>=(caseItem.maxAttempts||8)){store.updateCase(caseItem.id,{status:'failed'});logDetectives({ok:false,caseId:caseItem.id,phase:'exhausted',attempts});return{ok:false,reason:'exhausted'};}store.updateCase(caseItem.id,{status:'pending'});return{ok:false,reason:'no_pass_yet',attempts};}
function shouldHandoff(reason,payload){return handoff.shouldHandoff(reason,payload);}
async function handoffToDetectives(payload={}){if(!detectivesEnabled())return{ok:false,reason:'detectives_disabled'};if(!shouldHandoff(payload.skipReason,payload))return{ok:false,reason:'skip_not_eligible'};const added=store.addCase(payload);logDetectives({ok:false,phase:'handoff',caseId:added.case?.id,created:added.created,skipReason:payload.skipReason,skipStage:payload.skipStage,refreshed:!!added.refreshed});if(!added.created&&added.case&&['pending','failed'].includes(added.case.status)){try{const queueDoc=require('../x-autoposter-store').loadQueue();const replay=await investigateCase(added.case,queueDoc);return Object.assign({ok:true,replayed:true,resolution:replay},added);}catch(e){return Object.assign({ok:true,replayed:false,error:e.message},added);}}return Object.assign({ok:true},added);}
async function processDetectivesPile({limit=3,doc=null}={}){if(!detectivesEnabled())return{ok:true,skipped:true,reason:'detectives_disabled'};const queueDoc=doc||require('../x-autoposter-store').loadQueue();try{store.recoverStaleInvestigatingCases();}catch{}function dismissJunkPending(){const junk=store.listCases({status:'pending',limit:100}).filter((c)=>handoff.isDismissibleCase(c));for(const row of junk){store.updateCase(row.id,{status:'expired',resolvedPath:'auto_dismissed'});store.appendLog(row.id,{phase:'dismiss',reason:'not_eligible'});}return junk.length;}const pending=handoff.sortCasesForProcessing(store.listCases({status:'pending',limit:Math.max(limit,50)}).filter((c)=>!handoff.isDismissibleCase(c))).slice(0,limit);dismissJunkPending();const results=[];for(const caseItem of pending){const out=await investigateCase(caseItem,queueDoc);results.push(Object.assign({caseId:caseItem.id},out));if(out?.queued)break;}return{ok:true,processed:results.length,results,counts:store.countByStatus()};}
async function tryImmediateResolve(handoffResult,doc){if(!handoffResult?.case?.id)return null;const caseItem=store.getCase(handoffResult.case.id);if(!caseItem||caseItem.status==='resolved')return null;return investigateCase(caseItem,doc);}
module.exports={detectivesEnabled,handoffToDetectives,processDetectivesPile,investigateCase,tryImmediateResolve,shouldHandoff,extractHints,resolveIdentity,buildStrategyCandidates,markDetectivesCandidate,buildBeatDrivenCandidate,formatResearchContextLine,formatResearchInsiderLine,formatBeatDrivenInsiderLine,countByStatus:store.countByStatus,listCases:store.listCases};
