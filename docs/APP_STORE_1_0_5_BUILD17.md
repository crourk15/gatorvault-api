# App Store 1.0.5 — Build 17

**Status:** Ready to upload via Codemagic (build number bump after 16 already used).

**Version:** marketing 1.0.5 · build 17
**Bundle:** com.gatorvaultinsider.app

## In this binary (on top of 16)

1. Menu → Podcasts stays in vault (`/vault/live/podcasts/`)
2. Game Zone Swamp Eve hub (cover call + score ticket + keys)
3. Push profile refresh on Codemagic

## Codemagic

Start **iOS Release Build** on `main` after this commit. App Store Connect rejected 16 as duplicate `CFBundleVersion`.
