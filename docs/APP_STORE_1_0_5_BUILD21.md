# App Store 1.0.5 — Build 21

**Status:** Ready after Build 20 TestFlight.

**Version:** marketing 1.0.5 · build 21  
**Bundle:** `com.gatorvaultinsider.app`

## In this binary (on top of 20)

1. **Membership & Account native fix** — Stop bare `/join/` redirects that fall back to the landing page in Capacitor; return to Membership after re-auth; clearer retry on load failures.
2. Everything from Builds 18–20.

## Codemagic

Start **iOS Release Build** on `main` after Build 20 is on TestFlight (or if 20 failed/cancelled).
