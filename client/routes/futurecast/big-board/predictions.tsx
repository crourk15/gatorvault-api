/** Predictions tab — UF commit probability. Spec §4.1 */
import React from 'react';

export default function PredictionsTab(): React.ReactElement {
  // TODO(Phase 4): GET /api/futurecast/big-board?tab=predictions
  return <div data-testid="tab-predictions">TODO: Predictions — spec §4.1</div>;
}
