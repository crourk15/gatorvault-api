'use client';

import React from 'react';
import { FreeVsInsider } from '@/components/welcome/FreeVsInsider';

/** @deprecated Use FreeVsInsider on Welcome — kept for /insider imports. */
export function InsiderComparisonTable(): React.ReactElement {
  return <FreeVsInsider />;
}
