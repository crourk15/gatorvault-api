'use client';

import React from 'react';
import { PlayerDirectoryPage } from '@/components/site/PlayerDirectoryPage';

export default function PlayersRoute(): React.ReactElement {
  return <PlayerDirectoryPage />;
}
