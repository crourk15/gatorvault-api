import React from 'react';
import '@/lib/uf-premium-gnl.css';

export default function GatorNationLiveRouteLayout({
  children,
}: {
  children: React.ReactNode;
}): React.ReactElement {
  return <>{children}</>;
}
